import React from 'react'
import { View, ActivityIndicator } from 'react-native'

export const SplashScreen = () => {
    return (
        <View>
            <ActivityIndicator/>
        </View>
    )
}
